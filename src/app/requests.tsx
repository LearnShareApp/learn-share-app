import { StyleSheet, Text, View } from "react-native";

const Requests = () => {
  return (
    <View>
      <Text>requests</Text>
    </View>
  );
};

export default Requests;

const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
  },
});
