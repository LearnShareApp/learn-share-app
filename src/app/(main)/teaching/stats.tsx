import { StyleSheet, Text, View } from "react-native";
const Stats = () => {
  return (
    <View>
      <Text>About</Text>
    </View>
  );
};

export default Stats;

const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
  },
});
