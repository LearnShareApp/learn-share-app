import { StyleSheet, Text, View } from "react-native";
const Request = () => {
  return (
    <View>
      <Text>About</Text>
    </View>
  );
};

export default Request;

const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
  },
});
